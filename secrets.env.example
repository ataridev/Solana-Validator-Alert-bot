# ============================================================================
#  Bot secrets. Copy to secrets.env and fill in your own values.
#    cp secrets.env.example secrets.env
#  secrets.env is in .gitignore and will not be committed.
# ============================================================================

# Bot token from @BotFather
BOT_TOKEN="1111555555:AAAAAdd1fftggLhhHjPkmluvq"

# Chat for alarms (delinquency, balance, lost connectivity). Enable sound there.
# Get the ID via @username_to_id_bot. Do not drop the leading minus!
CHAT_ID_ALARM="-111111111"

# Chat for the info summary (dashboard, epoch, SFDP/KYC). Sound can be off.
CHAT_ID_INFO="-111111112"
